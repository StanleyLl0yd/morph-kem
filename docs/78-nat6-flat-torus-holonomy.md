# 78 — NAT6 flat-torus A5 global-holonomy control

## Status

**NAT6 is rejected by NAT-A007.** Changing the carrier from a sphere to an irregular torus successfully creates exactly flat connections with genuine global holonomy, but the hidden relation collapses to a constant-size public `pi_1 -> A5` representation problem. Public tree normalization followed by enumeration of the 30 legal ordered commuting-involution pairs recovers the unique verifier-accepted pair and normalized gauge on every measured instance.

NAT6 is not a trapdoor primitive, KEM, one-way function, post-quantum assumption, or production-security construction.

## Construction

Reuse the G16 flip-mixed irregular torus families `6x6`, `6x9`, and `8x9`. From the public triangulation compute two deterministic independent binary 1-cocycles `alpha,beta` spanning `H^1(T^2; F2)` modulo coboundaries.

Choose a seeded ordered pair of distinct commuting involutions `x,y in A5`. These generate a Klein-four subgroup. For each canonically oriented public edge `e=(u,v)` define

```text
c_e = x^alpha[e] y^beta[e].
```

Because `alpha,beta` are cocycles and `x,y` commute with order two, every triangular face has identity core holonomy while noncontractible torus cycles can retain nontrivial global holonomy.

Hide the core under a normalized seeded vertex gauge `g_v in A5`:

```text
observed_uv = g_u^-1 c_uv g_v,
g_0 = 1.
```

The verifier accepts any normalized `A5` gauge and any ordered pair of distinct commuting involutions that exactly reproduces all public edges. It never compares to the planted decomposition.

## NAT-A007

The public attack has two parts.

First, choose a deterministic spanning tree and integrate the observed connection as though the tree core were identity. Gauge-transform every public edge by this tree integration. All tree residuals become identity; non-tree residuals expose the finite set of global loop holonomies.

Second, enumerate the complete public set of ordered distinct commuting involution pairs in `A5`. For each pair:

1. build the public core labels from `alpha,beta`;
2. propagate the unique normalized vertex gauge along the public spanning tree;
3. recompute every public edge exactly;
4. retain every verifier-accepted decomposition.

There are only 30 ordered legal holonomy pairs in `A5`. The attack does not enumerate vertex-label assignments.

## Fixed Python 3.12 result

For `nat6-8x9`:

```text
V/E/F:                              72 / 216 / 144
Euler characteristic:                         0
edge incidence min/max:                    2 / 2
H1 dimension:                                  2
alpha / beta weights:                    20 / 23
nonidentity face holonomies:                   0
tree / non-tree edges:                   71 / 145
normalized nonidentity residuals:              39
normalized distinct nonidentity residuals:      3
commuting pair candidates / tested:       30 / 30
vertex propagation assignments:              2130
accepted decompositions:                        1
first accepted pair matches planted:          yes
first accepted gauge matches planted:         yes
```

The three distinct nonidentity residual values are exactly the three nontrivial elements of the Klein-four holonomy subgroup generated by the selected pair.

## Eight-seed / multi-size sweep

Across `nat6-6x6`, `nat6-6x9`, and `nat6-8x9` × eight deterministic seeds (**24 public instances**):

- Euler characteristic is 0 on **24/24**;
- every edge is incident to exactly two triangles on **24/24**;
- measured public `H^1` dimension is 2 on **24/24**;
- every triangular face holonomy is identity on **24/24**;
- tree normalization exposes exactly **3 distinct nonidentity residual values on 24/24**;
- exactly 30 legal global-holonomy pairs are tested on every instance;
- exactly **one** verifier-accepted pair/gauge decomposition is found on **24/24**;
- after public success, the accepted holonomy pair matches planted on **24/24**;
- after public success, the accepted normalized vertex gauge matches planted on **24/24**.

Public work is linear in the carrier size times a constant 30-pair factor. Gauge-propagation assignments are exactly:

```text
6x6: 30 * 35 = 1050
6x9: 30 * 53 = 1590
8x9: 30 * 71 = 2130
```

The number of nonidentity tree-normalized residual edges grows with the carrier, but the set of distinct values remains the same three-element nontrivial Klein-four subgroup.

## Interpretation

NAT6 confirms the expected topological distinction from NAT4: flatness on a torus is not pure gauge. Genuine global holonomy survives.

However, with a fixed finite structure group and a verifier that permits only a pair of commuting involutions on the two public torus classes, the remaining hidden information is tiny. Public spanning-tree normalization exposes the global representation, and exhaustive recovery needs only 30 candidate pairs rather than a search over `A5^V` vertex gauges.

This is a structural rejection of the current fixed-holonomy architecture. Increasing only the torus dimensions does not change the constant-size global representation search and is not a repair.

A successor would need a global relation whose representation variety itself grows nontrivially and still survives public conjugacy/gauge normalization, finite representation quotients and generic constraint solving. That is a new relation, not a parameter increase.

No security claim.
