name: G11 HGES toroidal hypercover negative control

on:
  pull_request:
    paths:
      - 'src/morph_kem/gluing_surface_hypercover.py'
      - 'tests/test_gluing_surface_hypercover.py'
      - 'scripts/gluing_surface_hypercover_*.py'
      - 'docs/05-cryptanalysis.md'
      - 'docs/46-g11-toroidal-hypercover.md'
      - 'notes/research-log.md'
      - 'README.md'
      - '.github/workflows/g11.yml'
  push:
    branches: [main]
    paths:
      - 'src/morph_kem/gluing_surface_hypercover.py'
      - 'tests/test_gluing_surface_hypercover.py'
      - 'scripts/gluing_surface_hypercover_*.py'
      - 'docs/05-cryptanalysis.md'
      - 'docs/46-g11-toroidal-hypercover.md'
      - 'notes/research-log.md'
      - 'README.md'
      - '.github/workflows/g11.yml'

permissions:
  contents: read

jobs:
  g11:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.11', '3.12', '3.13']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Compile G11
        env:
          PYTHONPATH: src
        run: python -m compileall -q src/morph_kem/gluing_surface_hypercover.py tests/test_gluing_surface_hypercover.py scripts/gluing_surface_hypercover_baseline.py scripts/gluing_surface_hypercover_sweep.py scripts/gluing_surface_hypercover_sat.py
      - name: G11 unit tests
        env:
          PYTHONPATH: src
        run: python -m unittest tests.test_gluing_surface_hypercover -v
      - name: G11 P3 hypercover baseline
        env:
          PYTHONPATH: src
        run: python scripts/gluing_surface_hypercover_baseline.py --params g11-6x9 --solution-cap 64
      - name: G11 multi-seed sweep
        if: matrix.python-version == '3.12'
        env:
          PYTHONPATH: src
        run: python scripts/gluing_surface_hypercover_sweep.py --seeds 8 --solution-cap 32
      - name: Install MiniSat for G11 cross-check
        if: matrix.python-version == '3.12'
        run: |
          sudo rm -f /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends minisat
      - name: G11 independent MiniSat cross-check
        if: matrix.python-version == '3.12'
        env:
          PYTHONPATH: src
        run: python scripts/gluing_surface_hypercover_sat.py --params g11-6x9 --solver minisat
